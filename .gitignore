*.bak-*
db
ecdict-*.zip
import_wb.txt.bak-gboard
wgime - Copy*.bat

# test artifacts: screenshots may contain chat-window content; dumps are debug logs
tests/*.png
tests/*-dump.txt
# third-party data, re-downloadable (URL in build-full-singles.ps1 header)
tests/pinyin-data.txt

plugins/lt.txt

# wgtray distribution branch: wgime.bat is only a LOCAL build source for
# build-wgtray.ps1 (restore from master with: git checkout master -- wgime.bat)
# anchored to the repo root only - wgime-dll/WgIme.bat must stay tracked
/wgime.bat

# generated per-machine launcher shortcuts (created on first launch)
/wg-all/WgIme.lnk
/wg-all/WgTray.lnk

# root IME dictionaries: local-only on this branch (all tables live inside WgIme.dll)
/py.txt
/wb.txt
/ec.txt
/import_py.txt
/import_wb.txt

/tray_cs.cs
