## WgIme v1.1.0 — 界面全面现代化 + 功能扩充

单文件 `wgime.bat` 下载即用（Windows 10/11，无需安装）。配置/插件/工具箱样例首次运行自动播种。

### 界面（全新设计语言：浅蓝灰底 + 白色卡片 + 深色控制台 + 细滚动条）
- **时钟插件重制**：无边框圆角窗体（可拖动标题栏）、秒针圆环、闹钟、整点报时、倒计时（圆环进度 + 5-45 分预设 + 自定义提醒）、秒表（毫秒 + 计次）、番茄统计（7 天柱状图）
- **便签重制**：Win11 Sticky Notes 风、6 色主题、**多标签**（最多 9 个，自动迁移旧 notes.txt）、自绘细滚动条
- **工具箱重制**：扁平标签条 + 圆角磁贴按钮 + 深色日志控制台；`[cols N]` 多列布局；磁贴溢出自动出细滚动条（滚轮直接滚）
- **网络工具重制**：统一现代风，深色控制台
- **计算器**：迁出为 `[csharp]` 插件并全面现代化（磁贴按键/键盘直输/表达式历史）

### 功能
- **网络工具大幅扩充**：DNS 查询（手写原始协议 UDP/53，A/AAAA/CNAME/MX/TXT/NS/PTR）、HTTP 检测（状态码/TTFB/总耗时）、常用端口扫描、公网 IP、ping 丢包率/时延统计 + 包大小可配、子网拆分 / IP 范围转最小 CIDR / 掩码速查表 / 地址类型判断
- **插件支持 WPF**：`[csharp]` 插件引用新增 PresentationFramework 等，可直接 `new Window().Show()` 写 WPF 窗体
- **confirm 扩展语法**：`confirm 文本 | title=标题 | buttons=yesno|okcancel|ok | default=1|2`
- **app = 启动器**：相对路径按 bat 所在目录解析；命令/参数支持 %环境变量%
- **联想交互修正**：联想行只认数字键选中，空格照常输入空格（不再被劫持），带 `↪联想` 标记

### 修复
- 圆角窗体角落残像素（改 GDI 原生 Rgn + 描边内缩）
- 自绘按钮暗边（原生 BUTTON 主题边框渗出，改 Panel 基类）
- 圆环文字被透明面板盖住（改为 Paint 内自绘）
- 主按钮按下崩溃（颜色通道钳制）
- 滚动条抖动（双缓冲 + 变化才重绘）
- 番茄图星期标签右偏

### 文档
- `docs/` 三份文档同步更新；tools.txt / plugins\README.txt 头注释补充脚本块实战注意点
